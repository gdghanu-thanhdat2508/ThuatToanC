#include <bits/stdc++.h>
using namespace std;
using ll = long long;

solve(int n)
{
    queue<string> q;
    q.push('8');
    int ans = 0;

    while (!q.empty())
    {
        }
}
int main()
{
    // Cho so tu nhien N, tim so nguyen duong X nho nhat duoc tao boi so 8 va 0 chia het cho N
    // VD : N = 5 => X = 80 chia het cho 5

    return 0;
}