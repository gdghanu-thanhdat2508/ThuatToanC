#include <bits/stdc++.h>
using namespace std;
using ll = long long;

struct SinhVien
{
    string ten;
    string ns;
    string dc;
    int t, l, h;
};

void nhap(SinhVien &s)
{
    getline(cin, s.ten);
    getline(cin, s.ns);
    // cin >> s.gpa;
}

int main()
{
    // nhận được 1 danh sách học sinh gồm thông tin về tên, ngày sinh, địa chỉ, điểm thi đại học
    // của 3 môn Toán, Lý, Hóa. Nhiệm vụ của bạn dựa trên điểm trúng tuyển của ngành CNTT là 24 để xác định bạn
    // nào trượt, bạn nào đỗ. Ngoài ra bạn còn phải tìm ra thủ khoa ngành CNTT năm nay.

    return 0;
}